AES Encryption with 128-bit long key

To Run:
	1) Type "make" in the command line, press ENTER
	2) Then type "./AESEncrypt128 *YOUR file's absolute path*", press ENTER
	3) DONE. In the forlder with original file,
		new file with ".enc" extention has been added.
